/* 6. Set data structure
Create a function named setFromArray that returns a Set from an array.
It accepts an argument (Array, of any kind of element).
*/

export default function setFromArray(item) {
  return new Set(item);
}
